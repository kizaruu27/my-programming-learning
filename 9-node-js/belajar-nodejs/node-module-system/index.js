const fs = require('fs'); // import core module
// const showData = require('./app'); // import local module
// const moment = require('moment'); // third party module / npm module / node_modules

const data = require('./app')
// console.log(data); 
console.log(data.Data('Ucup'), data.PI, data.mahasiswa.cetakData(), new data.Manusia());